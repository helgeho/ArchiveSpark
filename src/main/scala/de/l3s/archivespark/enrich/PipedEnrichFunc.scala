package de.l3s.archivespark.enrich

/**
 * Created by holzmann on 07.08.2015.
 */
//class PipedEnrichFunc[Root <: EnrichRoot[_, Root, Target], Source <: Enrichable[_, Source, Target], Target <: Enrichable[_, _, Target]]
//(parent: DependentEnrichFunc[Root, Source, Target], val dependency: EnrichFunc[Root, _, Source]) extends EnrichFunc[Root, Source, Target] {
//  override def source: Seq[String] = parent.source
//  override def derive(source: Source): Map[String, Target] = parent.derive(source)
//  override def field: IdentityMap[String] = parent.field
//}
